kind = "web"
previewPath = "/btc-backtest/"
title = "BTC Backtest Calculator"
version = "1.0.0"
id = "artifacts/btc-backtest"
router = "path"

[[services]]
name = "web"
paths = [ "/btc-backtest/" ]
localPort = 5000

[services.development]
run = "streamlit run app.py --server.port 5000 --server.baseUrlPath /btc-backtest"

[services.env]
PORT = "5000"
